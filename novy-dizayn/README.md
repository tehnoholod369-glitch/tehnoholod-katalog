# Редизайн сайта — параллельная выкладка (заведено 24.08.2026)

Копия рабочего бандла из `../../САЙТ_НОВЫЙ_ДИЗАЙН/`: пять страниц (`.dc.html`),
их общие скрипты (`cart.js`, `search.js`, `support.js`, `mobile-bar.js`, `photo-base.js`)
и данные (`data/`). Ничего не переименовано — ссылки между страницами и так работают.

**Старый сайт эта папка не трогает.** Решение владельца 24.08.2026: сначала новые страницы
заводятся рядом со старыми, проверяются, и только потом старое удаляется — не одним
большим взрывом. Подробности и статус — `00_ГДЕ_ПРАВДА.md`, §7 (репозиторий
`~/Claude/Projects/Единый источник данных/`).

Обновлять эту папку — заново скопировать из `САЙТ_НОВЫЙ_ДИЗАЙН/` после очередной
пересборки (`gen_site_data.py`), не редактировать здесь напрямую: правки не переживут
следующее обновление.
