# Индекс каталога — производная от katalog-*.html, в git не хранится:
# он пересобирается сам при первом обращении, если страницы новее индекса.
# Хранить его в репозитории значило бы завести второй источник данных,
# который умеет разъезжаться с витриной.
catalog-index.json
catalog-meta.json
