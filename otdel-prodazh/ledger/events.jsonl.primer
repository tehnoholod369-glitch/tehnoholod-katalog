// Реестр сделок отдела продаж ИП «ТехноХолод». Только дозапись.
// Пишется через tools/ledger.py, руками не редактируется: правка — это новое событие.
// Строки, начинающиеся с //, читателем пропускаются.
//
// Это заготовка. Настоящий журнал — events.jsonl рядом, он в git не попадает: в нём
// имена и адреса клиентов, а репозиторий публичный. Нет events.jsonl — скопируйте
// этот файл под именем events.jsonl, и отдел заработает с чистой историей.
