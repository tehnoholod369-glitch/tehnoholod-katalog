# Реестр сделок в git не хранится. Репозиторий каталога публичный: его отдаёт jsDelivr,
# так витрина получает фотографии. Событие `lead` содержит имя клиента, его контакт, адрес
# объекта и текст обращения дословно — публиковать это нельзя.
#
# Журнал живёт на машине владельца, рядом с пайплайном PIM, и в бэкап попадает вместе с
# мастер-таблицей. Здесь остаётся только заготовка первой строки.
events.jsonl
